apiVersion: v1
kind: ConfigMap
metadata:
  name: bills-config
  namespace: bills-app
data:
  # API configuration
  PORT: "3002"
  NODE_ENV: "production"
  WEB_URL: "http://bills-web-svc"
  # Web configuration — browser accesses via ingress
  NEXT_PUBLIC_API_URL: "http://bills.local/api/v1"
  NEXTAUTH_URL: "http://bills.local"
  # Server-side API URL (pod-to-pod, bypasses ingress)
  API_INTERNAL_URL: "http://bills-api-svc:3002/api/v1"
  # PostgreSQL
  POSTGRES_DB: "bills_db"
  POSTGRES_USER: "bills"
